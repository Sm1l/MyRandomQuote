{
  "name": "homeworkmyrandomquote",
  "version": "1.0.0",
  "description": "",
  "source": "src/index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rm -rf dist && parcel",
    "build": "rm -rf docs && parcel build --public-url='/MyRandomQuote/' --dist-dir=docs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.0",
    "parcel": "2.8.0"
  }
}
